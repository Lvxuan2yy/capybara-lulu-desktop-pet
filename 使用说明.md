# 水豚噜噜桌面宠物 v0.1

一只住在你桌面上的水豚噜噜。会发呆、眨眼、到处闲逛、打盹，能拖动、能摸头。

## 怎么启动

双击项目目录里的 **`启动桌宠.vbs`**（不会弹黑色控制台窗口）。
首次启动后噜噜会出现在屏幕右下角，以后会记住你上次摆放的位置。

- 调试模式（想看报错日志）：双击 `启动调试(有日志窗口).bat`
- 想开机自动出现噜噜：右键噜噜 → 勾选「开机自动启动」

## 怎么退出

右键噜噜（或右键任务栏托盘里的噜噜图标）→「退出」。
直接关机/注销也会自动结束。

## 互动方式

| 操作 | 效果 |
| --- | --- |
| **左键按住拖动** | 把噜噜拎起来挪到任意位置，松手放下（它会委屈地晃腿） |
| **左键单击** | 摸头：噜噜会开心地蹦一下，冒出一句台词 |
| **不管它** | 会自己在屏幕底部来回散步；约 35 秒没人理就会趴着睡觉、冒 Zzz |
| **右键噜噜 / 托盘图标** | 菜单：摸摸噜噜、出去散步、原地待着、小睡一会、大小、开机自启、退出 |

## 噜噜的五种状态

- **待机 idle**：站着呼吸、随机眨眼，偶尔害羞/傻笑
- **散步 walk**：沿屏幕底部左右走，到边缘自动转身
- **被拎起 drag**：被鼠标拖动时四肢下垂、轻轻晃
- **开心 happy**：被摸头后蹦跳 + 台词气泡
- **睡觉 sleep**：趴着打盹、冒 Zzz，点一下就叫醒

## 目录结构

```
水豚噜噜桌宠\
├── 启动桌宠.vbs              正式启动入口（双击它）
├── 启动调试(有日志窗口).bat   调试入口
├── lulu_pet.py               桌宠主程序（状态机、动画、交互都在这）
├── 使用说明.md               本文件
├── config.json               运行后自动生成（位置、大小设置，可删）
├── assets\
│   ├── pet\                  程序实际使用的 8 张透明 PNG 素材
│   └── raw\                  素材原图（AI 生成图 + 原始立绘）
├── tools\
│   ├── build_assets.py       白底立绘 → 透明 PNG 的抠图脚本
│   ├── preview_states.py     渲染各状态预览图（开发用）
│   └── verify_menu.py        菜单/自启/缩放自检（开发用）
└── logs\pet.log              运行日志（出问题先看这里）
```

## 运行环境

- Windows 10/11
- Python 3.10+（本机已安装在 `D:\python\python.exe`）
- 依赖：PySide6、Pillow、numpy

换电脑或环境损坏时，一条命令装好依赖：

```powershell
D:\python\python.exe -m pip install PySide6 Pillow numpy
```

## 以后想加新玩法（给后续开发留的口子）

1. **加新动作素材**：把白底立绘放进 `assets\raw\`，命名如 `dance_raw.png`，
   在 `tools\build_assets.py` 的 `TARGETS` 里加一行，运行
   `D:\python\python.exe tools\build_assets.py` 即生成透明 PNG。
2. **加新状态**：在 `lulu_pet.py` 顶部加状态常量，
   在 `enter_state()`、`tick()`、`_current_visual()` 三处各加对应分支即可，
   动画帧、气泡、Zzz 的绘制方式可直接参考现有状态。
3. **加新台词**：改 `HAPPY_LINES / WAKE_LINES / WALK_LINES` 三个列表。

后续可扩展方向：喂食与橘子道具、番茄钟专注模式、桌面快捷动作、
换装（小鸟包/河豚包）、触摸反应、节日皮肤等。
